# Age encryption private key (never commit!)
# Back this up securely - without it you cannot decrypt your secrets
key.txt

.opencode

# devenv artifacts (should not be in dotfiles repo)
.devenv/
devenv.lock

# Zellij plugins (downloaded via manifest, not tracked in git)
home/dot_config/zellij/plugins/*.wasm

# Agent output
plan.md
docs/superpowers/
