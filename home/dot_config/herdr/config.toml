onboarding = false

[theme]
# Use terminal colors directly — inherits from Ghostty's "Default Style Dark"
name = "nord"

[terminal]
# Use default $SHELL (fish)
new_cwd = "follow"

[keys]
prefix = "ctrl+a"
settings = "prefix+."
reload_config = "prefix+r"
help = "prefix+?"
# Session/workspace
detach = "prefix+x"
workspace_picker = "prefix+w"
switch_workspace = "prefix+shift+1..9"
navigate_workspace_up = "up"
navigate_workspace_down = "down"
rename_workspace = "prefix+shift+r"
# Navigate-mode pane focus (while picker is open)
navigate_pane_up = "i"
navigate_pane_down = "k"
navigate_pane_left = "j"
navigate_pane_right = "l"
switch_tab = "alt+1..9"
new_tab = ["ctrl+shift+t", "prefix+n"]
previous_tab = "prefix+i"
next_tab = "prefix+k"
focus_pane_left = "alt+left"
focus_pane_down = "alt+down"
focus_pane_up = "alt+up"
focus_pane_right = "alt+right"
# focus_agent = "alt+1..9"
split_vertical = ["prefix+e"]
split_horizontal = ["prefix+d"]
edit_scrollback = "prefix+a"
zoom = "prefix+z"
new_workspace = "alt+n"
toggle_sidebar = "ctrl+b"
# close_pane = "prefix+x"
# close_tab = "prefix+shift+x"
rename_tab = "prefix+r"
# rename_pane = "prefix+shift+p"
close_workspace = "prefix+shift+D"
resize_mode = "prefix+shift+z"

# Custom commands
[[keys.command]]
key = "alt+shift+g"
type = "pane"
command = "gitui"

# [[keys.command]]
# key = ""
# type = "pane"
# command = "btop"

[ui]
mouse_capture = true
prompt_new_tab_name = true
show_agent_labels_on_pane_borders = false
# Show all agents across workspaces in the sidebar
agent_panel_scope = "all"
accent = "#FFD787"

[ui.toast]
# In-app toast notifications for background agent state changes
delivery = "herdr"

[ui.sound]
# Disable sounds (quiet workflow)
enabled = false

[experimental]
# Kitty graphics for inline images (Ghostty supports kitty protocol)
kitty_graphics = true

[advanced]
# 10MB scrollback per pane (default, explicit for clarity)
scrollback_limit_bytes = 10000000

