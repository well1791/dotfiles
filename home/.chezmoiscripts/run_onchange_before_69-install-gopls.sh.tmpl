{{ template "install-header.sh.tmpl" . }}
# Verify gopls (installed via home-manager)
# https://pkg.go.dev/golang.org/x/tools/gopls

if log_version gopls; then exit 0; fi
log_warn "gopls not found — ensure home-manager has been applied (run: home-manager switch --flake ~/.config/home-manager)"
