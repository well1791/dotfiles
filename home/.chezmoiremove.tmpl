{{/* Files that chezmoi should actively remove from the target.
     Add entries here when you stop using a tool or restructure configs.
     This ensures a clean target state on all machines.
     
     Format: one target path per line (relative to home).
     Supports globs.
*/}}
