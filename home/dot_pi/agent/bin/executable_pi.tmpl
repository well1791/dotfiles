#!/bin/sh
# Pi wrapper: Uses Nix-managed Node (home-manager) to avoid version conflicts
# Requires Node 20+ for regex v flag support
exec {{ .chezmoi.homeDir }}/.nix-profile/bin/node {{ .chezmoi.homeDir }}/.bun/bin/pi "$@"
