#!/bin/sh
# Pi wrapper: Forces latest Node to avoid compatibility issues with directory-specific Node versions
# Requires Node 20+ for regex v flag support
exec {{ .chezmoi.homeDir }}/.local/share/mise/installs/node/latest/bin/node {{ .chezmoi.homeDir }}/.bun/bin/pi "$@"
